import React from "react";
import { IMessage } from "../type";

type Props = {
    messages: IMessage[]
}

export const Messages:React.FC<Props> = ({messages}) => {
    return (
        <>
        </>
    );
}